const path = require('path');

module.exports = {
  i18n: {
    defaultLocale: 'pl',
    locales: ['en', 'pl'],
    otherLanguages: ['en', 'pl'],
    defaultLanguage: 'pl',
    fallbackLng: ['pl'],
  },
};
